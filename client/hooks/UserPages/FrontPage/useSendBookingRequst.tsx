type useSendBookingRequestProps = {
	date: string;
	locationType: string;
	address: string;
};
// userId: string, // Get from async

export const useSendBookingRequest = ({
	date,
	locationType,
	address,
}: useSendBookingRequestProps) => {};
