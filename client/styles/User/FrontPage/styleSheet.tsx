import { StyleSheet } from 'react-native';

const fontPageStyles = StyleSheet.create({
	scrollViewRow: {
		paddingBottom: 20,
		flexDirection: 'row',
	},
	scrollViewColumn: {
		paddingRight: 90,
	},
});

export default fontPageStyles;
